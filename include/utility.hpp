#pragma once
#pragma once
#include <vector>
#include <numeric>
#include <stdexcept>

using std::vector;

namespace utility {
    float dotProduct(const vector<float>& emb1, const vector<float>& emb2);
}